<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <record id="milestone_bridal_specialist_template_id" model="mail.template">
            <field name="name">Milestone Bridal Specialist</field>
            <field name="email_from">${'noreply@localhost'|safe}</field>
            <field name="subject">Hello ${object.name}</field>
            <field name="model_id" ref="project_milestone.model_milestone_milestone"/>
            <field name="auto_delete" eval="True"/>
            <field name="body_html"><![CDATA[
            <div>
                <p>Dear ${object.name}</p>

                <p>This is the notify you for task of milestone</p>
            </div>
            ]]></field>
        </record>
    </data>
</odoo>