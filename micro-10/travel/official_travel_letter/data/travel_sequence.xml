<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<record id="sequence_official_travel" model="ir.sequence">
        <field name="name">Official Travel sequence</field>
        <field name="code">official.travel</field>
        <field name="prefix">OFTR/%(y)s/%(month)s/</field>
        <field eval="1" name="number_next"/>
        <field eval="1" name="number_increment"/>
        <field eval="True" name="use_date_range"/>
        <field eval="False" name="company_id"/>
        <field name="padding">6</field>
    </record>
    
    <record id="email_template_official_travel" model="mail.template">
    	<field name="name">Official Travel</field>
    	 <field name="email_from">${user.email|safe}</field>
    	 <field name="subject">Your Official travel letter has been created</field>
    	 <field name="email_to">${object.employee_id.user_id.email}</field>
    	 <field name="lang">${user.lang|safe}</field>
    	 <field name="model_id" ref="model_official_travel"/>
    	 <field name="body_html"><![CDATA[<html>
                <head></head>
                % set record = ctx.get('record')
                % set company = user.company_id
                <body>
					<hr/>
					<p>${company.name}</p>
					<p>${company.street},${company.street2}</p>
					<p>${company.city},${company.state_id.name}</p>
					<p>${company.country_id.name} - ${company.zip}</p>
					<p>Phone : ${company.phone}</p>
					<p>Fax : ${company.fax}</p>
					<p>Website : ${company.website}</p>
					<p>Email : ${company.email}</p>
					<br/><br/>
					<p><b>Official Travel Work Order</b></p>
					<p>No.   : ${object.name}</p>
					<br/>
					<p>In order to fulfill the needs of <b> ${object.company_id.name}</b>, therefore ${object.create_uid.name} is giving order to :<p>
					<p>Name         : ${object.employee_id.name} </p>
					<p>job Position : ${object.job_id.name} </p>
					<p>Department   : ${object.department_id.name} </p>
					<p>To           : ${object.company_id.name} </p>
					<p>Period       : ${object.from_date} - ${object.to_date} </p>
					<p>To do the tasks as listed here	:</p>
					<ul>
		% for job in object.job_desc_ids:
						<li style='margin-left:15px'>${job.name}</li>
		% endfor
					</ul>
					<br/>
					<p>This order is given to the person concerned to be carried out properly.</p>
					<br/>
					<p>${company.city} , ${object.create_date}</p>
					<p><b>${company.name}</b></p>
					<br/>
					<p><b>${object.create_uid.name}</b></p><hr/>
				</body>
				</html>
]]></field>
    </record>
</odoo>