odoo.define('pos_order_reservation.main', function (require) {
	var models = require('pos_order_reservation.models');
	var screens = require('pos_order_reservation.screens');
	var popups = require('pos_order_reservation.popups');
	var db = require('pos_order_reservation.db');
});