<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="random_generate_password.list">
      <field name="name">random_generate_password list</field>
      <field name="model">random_generate_password.random_generate_password</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="random_generate_password.action_window">
      <field name="name">random_generate_password window</field>
      <field name="res_model">random_generate_password.random_generate_password</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="random_generate_password.action_server">
      <field name="name">random_generate_password server</field>
      <field name="model_id" ref="model_random_generate_password_random_generate_password"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="random_generate_password" id="random_generate_password.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="random_generate_password.menu_1" parent="random_generate_password.menu_root"/>
    <menuitem name="Menu 2" id="random_generate_password.menu_2" parent="random_generate_password.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="random_generate_password.menu_1_list" parent="random_generate_password.menu_1"
              action="random_generate_password.action_window"/>
    <menuitem name="Server to list" id="random_generate_password" parent="random_generate_password.menu_2"
              action="random_generate_password.action_server"/>
    -->
  </data>
</odoo>