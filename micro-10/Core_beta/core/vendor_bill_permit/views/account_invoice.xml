<odoo>

    <record id="view_invoice_supp_form" model="ir.ui.view">
        <field name="model">account.invoice</field>
        <field name="inherit_id" ref="account.invoice_supplier_form"/>
        <field name="arch" type="xml">
            <field name="fiscal_position_id" position="after">
                <field name="permit_value_diplay" invisible="1"/>
                <field name="permit_value" attrs="{'invisible':[('permit_value_diplay', '!=', True)],
                    'required':[('permit_value_diplay', '=', True)],
                    'readonly': [('state','!=','draft')]}"/>
            </field>
        </field>
    </record>

</odoo>