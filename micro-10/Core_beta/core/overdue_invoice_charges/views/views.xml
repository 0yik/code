<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="overdue_invoice_charges.list">
      <field name="name">overdue_invoice_charges list</field>
      <field name="model">overdue_invoice_charges.overdue_invoice_charges</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="overdue_invoice_charges.action_window">
      <field name="name">overdue_invoice_charges window</field>
      <field name="res_model">overdue_invoice_charges.overdue_invoice_charges</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="overdue_invoice_charges.action_server">
      <field name="name">overdue_invoice_charges server</field>
      <field name="model_id" ref="model_overdue_invoice_charges_overdue_invoice_charges"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="overdue_invoice_charges" id="overdue_invoice_charges.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="overdue_invoice_charges.menu_1" parent="overdue_invoice_charges.menu_root"/>
    <menuitem name="Menu 2" id="overdue_invoice_charges.menu_2" parent="overdue_invoice_charges.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="overdue_invoice_charges.menu_1_list" parent="overdue_invoice_charges.menu_1"
              action="overdue_invoice_charges.action_window"/>
    <menuitem name="Server to list" id="overdue_invoice_charges" parent="overdue_invoice_charges.menu_2"
              action="overdue_invoice_charges.action_server"/>
    -->
  </data>
</odoo>