<odoo>
    <data>
        <record id="view_payment_term_form_inherit" model="ir.ui.view">
            <field name="name">account.payment.term.form.inherit</field>
            <field name="model">account.payment.term</field>
            <field name="inherit_id" ref="account.view_payment_term_form"/>
            <field name="arch" type="xml">
                <!-- find field description and add the field
                     idea_ids after it -->
                <xpath expr="//form/group" position="after">
                    <group>
                        <field name="currency_id" invisible="1"/>
                        <field name="overdue_charges_type" widget="radio" options="{'horizontal':true}"/>
                        <field name="overdue_type" style="width:30%"/>
                        <field name="overdue_percentage"  attrs="{'invisible': [('overdue_charges_type', '=', 'amount')]}" style="width:30%"/>
                        <!--<field name="overdue_type_amount" style="width:30%"-->
                               <!--attrs="{'invisible': [('overdue_charges_type', '=', 'percentage')]}"/>-->
                        <field name="rate" attrs="{'invisible': [('overdue_charges_type', '=', 'percentage')]}"
                               style="width:30%" widget="monetary" options="{'currency_field': 'currency_id'}"/>
                        <field name="computation_method" widget="radio" options="{'horizontal':true}"
                               attrs="{'invisible': [('overdue_charges_type', '=', 'amount')]}" style="width:30%"/>
                    </group>

                </xpath>
            </field>
        </record>
    </data>
</odoo>