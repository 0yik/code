<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="facility_booking" inherit_id="website.layout" name="HelpDesk Portal">
        <xpath expr="//li[@id='o_logout']" position="before">
            <li>
                <a href="/my/facility/booking" role="menuitem">Book Facility</a>
            </li>
        </xpath>
    </template>
    <template id="portal_fbo_submit" name="Booking Facility">
        <t t-call="website_portal.portal_layout">
            <div class="alert alert-success">
                <strong>Success!</strong> Your Facility Booking Order is Successfully Submitted.
            </div>
        </t>
    </template>
    <template id="portal_fbo_submit_fail" name="Booking Facility">
        <t t-call="website_portal.portal_layout">
            <div class="alert alert-danger">
                <strong>Errors!</strong> This Facility has already been booked from <t t-esc="booking_start_date"/> to <t t-esc="booking_end_date"/>
            </div>
        </t>
    </template>
    <template id="portal_my_fbo" name="My Booking">
        <t t-call="website_portal.portal_layout">
            <html>
                <head>
                    <style>
                        * {
                        box-sizing: border-box;
                        font-family: "Roboto","Helvetica Neue",Arial,sans-serif;
                        }

                        input[type=text], select, textarea {
                        width: 100%;
                        padding: 12px;
                        border: 1px solid #ccc;
                        border-radius: 4px;
                        resize: vertical;
                        }

                        label {
                        display: inline-block;
                        }

                        input[type=submit] {
                        background-color: #4CAF50;
                        color: white;
                        padding: 12px 20px;
                        border: none;
                        border-radius: 4px;
                        cursor: pointer;
                        float: right;
                        }

                        input[type=submit]:hover {
                        background-color: #45a049;
                        }

                        .container {
                        border-radius: 5px;
                        padding: 10px;
                        }

                        .col-25 {
                        float: left;
                        width: 25%;
                        margin-top: 6px;
                        }

                        .col-75 {
                        float: left;
                        width: 75%;
                        margin-top: 6px;
                        }

                        /* Clear floats after the columns */
                        .row:after {
                        content: "";
                        display: table;
                        clear: both;
                        }

                        /* Responsive layout - when the screen is less than 600px wide, make the two columns stack on top of each other instead of next to each other */
                        @media screen and (max-width: 600px) {
                        .col-25, .col-75, input[type=submit] {
                        width: 100%;
                        margin-top: 0;
                        }
                        }
                    </style>
                </head>
                <body>
                    <center><h3 style="margin-top: 0px;">Book Facility</h3></center>
                    <div class="container">
                        <form  action="/facility/submit/booking" method="post" name="booking_form" id="booking_form" class="o_website_form form-horizontal container-fluid" enctype="multipart/form-data">
                            <div class="row">
                                <div class="col-25">
                                    <label for="llocation">Location</label>
                                </div>
                                <div class="col-75">
                                    <select id="location" name="location" required="required">
                                        <option value="">Location...</option>
                                        <t t-foreach="location" t-as="l">
                                            <option t-att-value="l.id">
                                                <t t-esc="l.name" />
                                            </option>
                                        </t>
                                    </select>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-25">
                                    <label for="lfacility">Facility</label>
                                </div>
                                <div class="col-75">
                                    <select id="facility" name="facility" required="required">
                                        <option value="">Facility...</option>
                                        <t t-foreach="facility" t-as="f">
                                            <option t-att-value="f.id" t-att-location="f.location_id.id">
                                                <t t-esc="f.name" />
                                            </option>
                                        </t>
                                    </select>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-25">
                                    <label for="booking_start_date">Booking Start Date</label>
                                </div>
                                <div class="col-75">
                                    <input type="datetime-local" id="booking_start_date" name="booking_start_date" placeholder="Booking Start Date" required="required"/>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-25">
                                    <label for="booking_end_date">Booking End Date</label>
                                </div>
                                <div class="col-75">
                                    <input type="datetime-local" id="booking_end_date" name="booking_end_date" placeholder="Booking End Date" required="required"/>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-25">
                                    <label for="remarks">Remarks</label>
                                </div>
                                <div class="col-75">
                                    <textarea id="remarks" name="remarks" placeholder="Write something.."  style="height:200px"/>
                                </div>
                            </div>
                            <div class="row">
                                <input type="submit" value="Submit" />
                            </div>
                        </form>
                    </div>
                </body>
            </html>
        </t>
    </template>
</odoo>