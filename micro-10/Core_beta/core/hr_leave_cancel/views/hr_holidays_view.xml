<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        
        <record id="view_leave_cancel_button_hr_holidays_form" model="ir.ui.view">
            <field name="name">hr.holidays.form</field>
            <field name="model">hr.holidays</field>
            <field name="inherit_id" ref="hr_holidays.edit_holiday_new"/>
            <field name="arch" type="xml">
                <xpath expr="//button[@name='action_refuse']" position="after">
                    <button string="Cancel" icon=""
                        type="object" name="action_leave_cancel"
                        attrs="{'invisible': ['|', ('leave_cancel_id','!=', False), ('state', 'not in', ['validate'])]}"
                        />
                </xpath>
                <xpath expr="//field[@name='department_id']" position="after">
                    <field name="leave_cancel_id"
                        attrs="{'invisible': [('leave_cancel_id','=', False)]}"
                        context="{'from_cancel': True}"
                        readonly="1" />
                </xpath>
            </field>
        </record>

    </data>
</odoo>
